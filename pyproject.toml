[project]
name = "numpy-projects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hubert Liczbik", email = "liczbikhubert9@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "sympy>=1.14.0",
]

[project.scripts]
numpy-projects = "numpy_projects:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
